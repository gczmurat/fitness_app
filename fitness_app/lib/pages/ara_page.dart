import 'package:flutter/material.dart';

class AraPage extends StatelessWidget {
  const AraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Center(child: Text("Ara"),),);
  }
}